org.gradle.jvmargs=-Xmx4G -XX:MaxMetaspaceSize=2G -XX:+HeapDumpOnOutOfMemoryError
android.useAndroidX=true
android.enableJetifier=true
# This newDsl flag was added automatically by Flutter migrator
android.newDsl=false
# AGP 9 defaults to built-in Kotlin, but Flutter's Gradle plugin still applies the
# Kotlin Gradle Plugin to plugin subprojects, which AGP 9 then rejects. Opt out until
# https://docs.flutter.dev/release/breaking-changes/migrate-to-built-in-kotlin lands.
android.builtInKotlin=false
